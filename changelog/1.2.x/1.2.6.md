---
summary: "mcp-ts-core ^0.9.1 → ^0.9.6. Error factories in harvester (McpError → validationError/notFound). manifest.json and .mcpbignore scaffolded for MCPB bundle support. Skills synced. fast-xml-parser unpinned."
breaking: false
security: false
---

# 1.2.6 — 2026-05-23

## Changed

- **`@cyanheads/mcp-ts-core` ^0.9.1 → ^0.9.6** — picks up upstream fixes and improvements across the 0.9.x series.
- **`fast-xml-parser` unpinned** — removed the `^5.x` pin; now resolves to the latest compatible release.

## Added

- **MCPB bundle support** — `manifest.json` and `.mcpbignore` scaffolded. `bun run bundle` produces `dist/arxiv-mcp-server.mcpb` for one-click Claude Desktop install. `server.json` carries the MCPB package entry.
- **Error factory adoption in harvester** (`src/services/arxiv/mirror/harvester.ts`) — raw `McpError` constructions replaced with `validationError`/`notFound` factories for consistent error shape across the service layer.

## Maintenance

- Skills synced: maintenance 2.4, polish-docs-meta 2.2, release-and-publish 2.5, field-test 2.5.
- README install badges added; tool/resource descriptions rewritten to action-first voice; badge layout updated.
